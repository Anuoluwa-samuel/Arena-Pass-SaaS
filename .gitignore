# Dependencies
node_modules

# Next.js
.next/
out/
*.tsbuildinfo

# Environment — never commit secrets
.env
.env.*
!.env.example

# Local data (embedded Postgres, uploads). Anchored to the repo root: an unanchored
# `storage/` also matched the source folder server/storage/ and kept it out of git.
/.data/
/storage/

# Testing
coverage/
playwright-report/
test-results/

# OS / editor
.DS_Store
.vercel/

# Local screenshots from scripts/dev/screenshot.mjs
.shots/
